Use a command to output the ASM instructions from a headered NES file. e.g. "NES Disassembler.exe" "Super Mario Bros..nes"

Note: I have only tested this with Friday the 13th so far. It may not work with every nes rom yet. Also currently, it only works with iNES headered files.